-module(list).
-export([]).

%Return first N elements of Xs
take(Xs, N) ->
  list:sublist(Xs, N).

%Return Xs without N first elements
drop(Xs , N) ->
  .

%Return Xs with elements from Ys appended
append(Xs, Ys) ->
  .

%Check if Y is a member of Xs
member(Xs, Y) ->
  .

%Get the position of Y in Xs
position(Xs, Y) ->
  .
